[Service]
# PMSS defense-in-depth: per-user slice containment prevents pid exhaustion.
# Keep sshd favored for CPU/IO/OOM pressure so root can still recover hosts.
CPUWeight=10000
IOWeight=10000
OOMScoreAdjust=-1000
MemoryMin=64M
