# PMSS suspended customer stable host for ##username##.
server {
        listen 80;
        server_name ##serverNames##;
        root /var/www;

        location = /error-suspended.html {
                root /var/www;
        }

        location / {
                return 302 /error-suspended.html;
        }
}
